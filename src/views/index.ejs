<!doctype html>
<html data-theme="cupcake">
<head>
  <meta charset="utf-8" />
  <title><%= title %> · Portfolio</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />

  <!-- Basic SEO -->
  <meta name="description" content="Leo Atienza — portfolio of full-stack projects using Node/Express, EJS, Tailwind/daisyUI, and Postgres. See recent work, tech stack, and write-ups." />
  <link rel="icon" href="/favicon.ico" />

  <!-- Open Graph / Twitter -->
  <meta property="og:title" content="Leo Atienza · Portfolio" />
  <meta property="og:description" content="Full-stack projects, case studies, and links to code and live demos." />

  <!-- Styles -->
  <link rel="stylesheet" href="/css/main.css" />
</head>
<body>
  <%- include('partials/header') %>

  <main class="container mx-auto px-4">
    <!-- Hero centered in the middle -->
    <section class="min-h-[60vh] grid place-items-center text-center">
      <div class="max-w-3xl py-10">
        <h1 class="text-5xl font-bold mb-4">Hi, I’m Leo 👋</h1>
        <p class="opacity-80 mb-8 text-lg">
          Full-stack developer building with Node/Express, EJS, Tailwind/daisyUI, and Postgres.
        </p>
        <div class="flex flex-wrap gap-4 justify-center">
          <a class="btn btn-primary btn-lg" href="/projects">Projects</a>
          <a class="btn btn-ghost btn-lg" href="/about">About</a>
        </div>
      </div>
    </section>

    <% if (typeof recent !== 'undefined' && recent && recent.length) { %>
      <section class="px-0 py-10">
        <h2 class="text-3xl font-semibold mb-4">Recent</h2>

        <div class="grid gap-6 sm:grid-cols-2 lg:grid-cols-3">
          <% recent.forEach(p => { %>
            <div class="card bg-base-100 shadow-md hover:shadow-lg transition">
              <div class="card-body">
                <h3 class="card-title">
                  <a href="/project/<%= p.slug %>"><%= p.title %></a>
                </h3>
                <% if (p.summary) { %>
                  <p class="opacity-80"><%= p.summary %></p>
                <% } %>

                <% if (p.techStack || p.tools) { %>
                  <div class="mt-2 space-x-2">
                    <% if (p.techStack) { %>
                      <% ('' + p.techStack).split(',').map(s => s.trim()).filter(Boolean).forEach(s => { %>
                        <span class="badge badge-outline"><%= s %></span>
                      <% }) %>
                    <% } %>
                    <% if (p.tools) { %>
                      <% ('' + p.tools).split(',').map(s => s.trim()).filter(Boolean).forEach(s => { %>
                        <span class="badge"><%= s %></span>
                      <% }) %>
                    <% } %>
                  </div>
                <% } %>
              </div>
            </div>
          <% }) %>
        </div>
      </section>
    <% } %>
  </main>

  <%- include('partials/footer') %>
</body>
</html>